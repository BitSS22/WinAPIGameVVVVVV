#pragma once

// Ό³Έν :
enum class ERenderOrder
{
	BACK_GROUND,
	PLAYER
};

enum class EColorType
{
	WHITE,
	CYAN,
	PINK,
	RED,
	BLUE,
	YELLOW,
	GREEN,
	GREY,
	SIZE
};

enum class EBackGroundType
{
	SPACE,
	HORIZONTAL,
	VERTICAL,
	TOWER,
	SIZE
};